version: '3.9'
services:
  tmusic-backend:
    build:
      context: ./backend
    container_name: tmusic-backend
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
